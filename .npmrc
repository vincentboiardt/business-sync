# Shamefully hoist dependencies to avoid issues with some packages
shamefully-hoist=true

# Use strict peer dependencies
strict-peer-dependencies=false

# Auto install peers
auto-install-peers=true

# Use node-linker=hoisted for better compatibility with Next.js
node-linker=hoisted
